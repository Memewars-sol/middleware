export const init = () => {
    // todo
    // check asset ownership
    // transfer building's account_id
    // when an account changes id, the building does not change id unless it's in a bundle
    // account sales only result in transfer of levels / resources etc
    // add webhook checks
}